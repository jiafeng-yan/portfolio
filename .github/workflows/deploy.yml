name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    # 北京时间 00:05 运行（UTC 16:05 前一天）
    - cron: '5 16 * * *'
    # 北京时间 12:05 运行（UTC 04:05）
    - cron: '5 4 * * *'

permissions:
  contents: write
  pages: write
  id-token: write

# 避免并发执行
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Puppeteer dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libnss3 \
            libatk1.0-0 \
            libatk-bridge2.0-0 \
            libcups2 \
            libdrm2 \
            libxkbcommon0 \
            libxcomposite1 \
            libxdamage1 \
            libxfixes3 \
            libxrandr2 \
            libgbm1 \
            libasound2t64 \
            libpango-1.0-0 \
            libcairo2

      - name: Scrape LeetCode data
        run: node scripts/scrape-leetcode.mjs
        env:
          PUPPETEER_SKIP_CHROMIUM_DOWNLOAD: 'false'

      - name: Scrape GitHub data
        run: node scripts/scrape-github.mjs
        env:
          # 需要 GH_PAT (Personal Access Token) 来获取用户贡献数据
          # 创建方法: GitHub → Settings → Developer settings → Personal access tokens → Fine-grained tokens
          # 权限: 只需要 Profile: Read
          # 然后在仓库的 Settings → Secrets → Actions 中添加 GH_PAT
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}

      - name: Build
        run: npm run build

      - name: Commit scraped data
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add src/data/leetcode.json src/data/github.json
          if git diff --staged --quiet; then
            echo "没有更改需要提交"
          else
            CURRENT_DATE=$(date '+%Y-%m-%d')
            git commit -m "chore: update LeetCode and GitHub data - $CURRENT_DATE"
            git push
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
